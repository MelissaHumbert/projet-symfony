{% extends 'base.html.twig' %}

{% block title %}Région{% endblock %}

{% block body %}
    <h1>Région</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ region.id }}</td>
        </tr>
        <tr>
            <th>Région</th>
            <td>{{ region.regionName }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('region_index') }}" class="btn btn-primary">Retour</a>

    <a href="{{ path('region_edit', {'id': region.id}) }}"class="btn btn-warning">Éditer</a>

    {{ include('region/_delete_form.html.twig') }}
{% endblock %}
