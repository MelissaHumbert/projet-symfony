{% extends 'base.html.twig' %}

{% block title %}Éditer une Region{% endblock %}

{% block body %}
    <h1 class="text-center m-3">Éditer une Region</h1>

    {{ include('region/_form.html.twig', {'button_label': 'Éditer'}) }}

    <a href="{{ path('region_index') }}"class="btn btn-primary">Retour</a>

    {{ include('region/_delete_form.html.twig') }}
{% endblock %}
