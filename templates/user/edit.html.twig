{% extends 'base.html.twig' %}

{% block title %}Éditer un utilisateur{% endblock %}

{% block body %}
    <h1 class="text-center m-3">Éditer un utilisateur</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Éditer'}) }}

    <a href="{{ path('user_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
